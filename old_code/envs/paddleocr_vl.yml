name: doc2md-paddleocr-vl
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - -r ../requirements.txt
      - pytest
      - paddleocr
      - paddlepaddle

# Phase 3 note:
# This manifest is a recommendation scaffold only. Heavyweight installs and
# model downloads are deferred to Phase 4 minimal online installation testing.
