# Introduction

The pdf2md project investigates how raw backend outputs can be normalised into
a canonical extraction evidence model.

This paragraph contains meaningful document text that the Plan 10 connector
validation can recognise as semantic content, not parser noise.

<--- Page Split --->

# Methods

We describe a multi-backend pipeline and compare its outputs with a LaTeX
ground truth corpus assembled from real academic preprints.

Each backend produces a markdown rendering plus a manifest file capturing
provenance and command details for reproducibility.
