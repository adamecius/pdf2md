node_modules/
dist/
.vite/
*.tsbuildinfo
.env.local

# Staged repo artefacts — generated by webui/scripts/stage-data.mjs
# before each build. Treated as build output, not source.
validator/public/api/

# Built declaration files for the shared workspace.
shared/dist/

