"""Factory priors shipped with the package.

One JSON file per backend, each a serialised
``CalibrationPriorDocument``. Loaded at runtime by
``pdf2md.models.priors.load_factory_prior``.

Files in this directory are package data, declared in
``pyproject.toml`` under ``[tool.setuptools.package-data]``.
"""
