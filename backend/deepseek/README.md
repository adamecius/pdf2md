# pdf2md DeepSeek backend
Run: `python backend/deepseek/pdf2md_deepseek.py -i test.pdf` -> `test.md`.
Default mode requires a local model via `--model-path` or `PDF2MD_DEEPSEEK_MODEL`.
No silent downloads. Add `--allow-download` only when intentionally enabling download-capable flow.
