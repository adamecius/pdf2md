# MOVED — this file is no longer the install entry point.
#
# Run the project's standard installer instead:
#
#     python backend/semantic/deepseek_vl2/setup.py
#
# (See backend/semantic/deepseek_vl2/README.md for the full workflow.)
#
# The conda env spec lives in `environment.yml` (note the different
# filename, matching the OCR extraction backends). This file is
# intentionally an invalid env-spec — `conda env create -f env.yaml`
# will fail fast with a YAML parse error, redirecting users to the
# documented path above.
INVALID: "use setup.py"
