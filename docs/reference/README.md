# Reference

Authoritative descriptions of contracts, CLI flags, and schemas.

- [Calibration priors](calibration-priors.md) — the three-level
  fallback (user → factory → uninformative), the calibrator CLI, and
  how priors flow into consensus scoring.
- [Datasets registry](datasets-registry.md) — schema of the external
  dataset registry, supported corpora, and the `pdf2md datasets` CLI.
- [Export formats](export-formats.md) — Docling JSON, RAG chunks,
  Markdown preview, and the legacy adapter pointer.

For the code-level API contract, read the docstrings in
`src/pdf2md/` directly.
