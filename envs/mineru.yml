name: doc2md-mineru
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - -r ../requirements.txt
      - pytest
      - mineru

# Phase 3 note:
# This manifest is planning scaffolding. Validate real dependency resolution in
# isolated environments during Phase 4.
